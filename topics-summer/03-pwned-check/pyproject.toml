[project]
name = "pwned-check"
version = "0.1.0"
description = "Password checker (by partial hash) for Have I Been Pwned"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
]
